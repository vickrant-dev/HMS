# Task 0.9 — Initialize Git Repo

**Goal:** Git init, `.gitignore`, initial commit, push to GitHub.

**Remote:** `https://github.com/vickrant-dev/HMS.git` → `main`

---

## Checklist

- [x] Update `.gitignore` — fixed `*.jar` to allow `lib/*.jar`, removed `CONTEXT/` and `plan/` from ignore, preserved `# Other` section for AI tooling
- [x] `git init` — repo initialized
- [x] `git add -A` — all source, lib, database, plan, config staged
- [x] `git commit -m "Initial setup and config."` — 31 files, 5605 insertions
- [x] `git remote add origin https://github.com/vickrant-dev/HMS.git`
- [x] `git push -u origin main` — pushed successfully
