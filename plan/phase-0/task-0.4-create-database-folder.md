# Task 0.4 — Create `database/` Folder

**Goal:** Create placeholder directory for `schema.sql` and `seed.sql` files (to be populated in Phase 1).

---

## Checklist

- [x] Create `plan/phase-0/task-0.4-create-database-folder.md` checklist
- [x] Create `database/` directory under project root
- [x] Verify `database/` folder exists

---

## Expected Structure After Completion

```
ead_cw/
├── database/           ← empty, ready for Phase 1
├── lib/
├── src/
├── plan/
├── build.xml
└── ...
```
